# DUNQ

Basketball shoes & socks built to fly. Live at [dunqproject.store](https://dunqproject.store).

Built with React + Vite, deployed to GitHub Pages.

## Develop

```bash
npm install
npm run dev
```

## Deploy

```bash
npm run deploy
```

Builds `dist/` and pushes to the `gh-pages` branch.
